var Vow = require('Vow');

module.exports = {

    /**
     * Маппинг модели из источника
     */
    map: function(modelName, source) {
        var promise = Vow.isPromise(source) || Vow.promise(source);

        return promise.then(function(data) {

        });
    }

};
